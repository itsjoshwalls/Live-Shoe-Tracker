# Copy to .env and fill with real values for local development
# Use SUPABASE_SERVICE_ROLE_KEY on the server (keeps elevated permissions server-side)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...REPLACE_WITH_SERVICE_ROLE_KEY
# Alternatively for quick local dev you can use the anon key (not for production)
SUPABASE_ANON_KEY=eyJ...REPLACE_WITH_ANON_KEY

# Server port (avoid colliding with Next.js which uses 3000)
PORT=4000
